{
  "name": "sandbox",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "sandbox/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/sandbox",
        "main": "sandbox/src/index.ts",
        "tsConfig": "sandbox/tsconfig.lib.json",
        "assets": ["sandbox/*.md"]
      }
    }
  }
}
